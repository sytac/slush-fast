describe('config of <%= module.fullNs %>', function () {
	'use strict';

	beforeEach(module('<%= module.fullNs %>'));

	/*
	it('should be defined', inject(function (  ) {
	  expect(  )
	  .toBeDefined();
	}));
	*/

});
